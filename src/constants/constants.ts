export const ITEM_LOAD = 15;

export const START_PAGE = 1;

export const TIME_OUT = 60 * 1000; // ms

export enum AuthorizationMode {
    PUBLIC = 0,
    ACCESS_TOKEN = 1,
}

export enum DialogType {
    ONE = 'One Button',
    TWO = 'Two Button',
}

export enum LanguageType {
    ENGLISH = 'en',
    VIETNAMESE = 'vi',
}
